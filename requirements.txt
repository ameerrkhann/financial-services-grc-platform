# Financial Services Cyber Risk Intelligence Platform
# Direct dependencies only — pip resolves the rest.
# Python 3.13. Note that riskquant is deliberately absent: it does not support
# Python 3.13, so FAIR is implemented directly with numpy in fair_engine.py.

# Core modelling and data
numpy==2.4.3
scipy==1.17.1
pandas==3.0.1

# Charts
matplotlib==3.10.8

# Vendor risk web app
streamlit==1.58.0

# Excel export and templates
openpyxl==3.1.5

# Executive summary PDF
reportlab==5.0.1

# Terminal output
tabulate==0.10.0
colorama==0.4.6

# Tests
pytest==9.1.1
